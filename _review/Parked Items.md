---
type: review
status: stable
owner: design-system
surfaces: [shared]
source: specified
last_reviewed: 2026-06-15
maturity_score: 88
tags: [orbit, design-brain, parked, accessibility]
---

# Parked Items

Use this note for valuable validation or source-linking work that should not block the
next Design Brain build-out.

## Parked For Now

| Item | Status | Why parked | Resume when |
| ---- | ------ | ---------- | ----------- |
| Human-confirmed screen-reader pass for benchmark routes | Parked | Automated semantic checks and browser visual accessibility pass already succeeded; the remaining check needs a real VoiceOver, NVDA, or JAWS session. | Before claiming final screen-reader PASS or raising accessibility maturity beyond the current score. |
| Chip interactive-hover tokenisation (design decision) | Parked | `packages/orbit/src/indicators/Chip.module.css` `.interactive:hover` uses `filter: brightness(0.97)` — a hardcoded effect — across ~16 variant × contrast background contexts × 2 themes, none of which have hover tokens. A faithful tokens-only fix is a **design decision**, not a swap: **Opt 1** per-variant hover tokens (~32 Figma-sourced values, matches the Button precedent, preserves status colour — recommended); **Opt 2** a systemic 1–2 token overlay (cheap interim, needs a visual pass across all variants); **Opt 3** reuse neutral hover (rejected — kills the status signal). Surfaced by the build-pipeline: `component-builder` escalated rather than invent values (2026-07-17). Same anti-pattern the Button/IconButton fix resolved (`claude/button-tokens-fix`). | Owner picks Opt 1 vs Opt 2 and sources the hover values (ideally Figma) → then a change request (`_review/Change Request Template.md`) for the token additions + the `packages/orbit/src/indicators/Chip.module.css` swap, built through the pipeline. |
| Orbit design-system gaps surfaced by the Commentary prototype (2026-07-17) | Parked | The `design-review` of the Commentary/RAID prototype (`claude/commentary-prototype`) flagged four genuine **library/token gaps** (not prototype defects): (1) **no `--orbit-radius-full` token** — pill/circle shapes (avatars, dot markers) have no tokenised path, forcing `border-radius: 50%` literals; (2) **`DateInput` exposes no `ariaDescribedBy`** prop — its inline error text can't be programmatically linked, unlike `Input` (a11y gap in the component); (3) **no reduced-motion convention / motion tokens** at the library level (`Toast` ships an unguarded slide-in) — new work has to hand-roll a `prefers-reduced-motion` guard each time; (4) **`@efficio/orbit/inspector` (`OrbitInspector`, AGENTS.md §2.8) does not exist** — prototypes can only disclose a placeholder. | Owner decides which to fix via change requests to the coded design system: add `--orbit-radius-full`; add `ariaDescribedBy` to `DateInput` (+ family); define a motion-token + reduced-motion convention; ship the `OrbitInspector` export. Each unblocks a class of prototype work. |
| Pattern contract: RAID / status-review (`commentary-raid`) | Parked | No pattern contract exists for a RAID / status-review / activity-log screen; the scout flagged it and `screen-builder` (opus) resolved the ambiguity ad-hoc. Decisions worth canonicalising so they aren't re-litigated per screen: **register row status → `StatusIndicator`, card/detail status → `Badge`, Type/Visibility → `Chip`, Impact → `Badge`**; **row expansion → master-detail** (Orbit `Table` has no accordion); a standard **Overlay header/footer/action scaffold** for capture; the RAID status taxonomy; and type-driven capture-field rules. Reference build: `claude/commentary-prototype`. | Promote the prototype's decisions into a new `commentary-raid` pattern under `design-brain/patterns/` (from `design-brain/patterns/_TEMPLATE.md`) via the contract flow; owner approves. |

## Next Active Focus

Continue with source-backed page patterns and golden examples. This gives agents more
product-shape context and is higher leverage than blocking on the human screen-reader
session right now.

<!-- graph-links:start — generated by tools/gen_graph_links.py; do not hand-edit -->
## Vault graph
[[_review/Change Request Template|Change Request Template]] · [[design-brain/patterns/_TEMPLATE|patterns _TEMPLATE]]
<!-- graph-links:end -->
