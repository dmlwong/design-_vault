---
type: definition-matrix
status: draft
owner: <concept-team member running the definition>
surfaces: [<ClauseIQ | MarketIQ | RFP Analytics | RFP Builder | Other / shared>]
source: product
last_reviewed: <YYYY-MM-DD>
tags: [orbit, discovery, definition-matrix]
---

# Scenario & Behaviour Matrix: <concept name>

> **Stage 2 of the pack chain.** Produced only for concepts whose Concept Pack verdict is
> Ready (or Ready-with-conditions) — this is where the concept team, the tool owner, and
> the data team agree how the thing *behaves* before the build team commits.
>
> **Authoring:** Excel is a fine working surface — consultants live there. This markdown
> file is the **canonical copy**; convert/paste on each revision (the golden example
> shows the shape: `discovery/definition/clauseiq-supplier-rounds.md`). The journey-flows
> page is **generated from this file** by `tools/build_journey_flows.py` — never hand-edit
> the generated page.
>
> **Seeding:** start from the Concept Pack's typed gap report — every `edge case` becomes
> an E-row, every `open question` becomes a C-item with its owner. Nothing gets
> re-discovered.

**Source Concept Pack:** `discovery/briefs/<the Ready brief>.md` · **Working draft:** v0.1 · <date>

## Key rules (agreed)

> Numbered rules every journey relies on. One `### R<n> · <name>` per rule; the body is a
> single paragraph. Rules are *agreed* — anything still open belongs in the C-list below.

### R1 · <rule name>
<What the rule is, in one paragraph. Name the systems and fields it touches.>

## Open clarifications (to confirm)

> Undecided items, each with a named owner. These block build, not definition — record
> them honestly rather than resolving them silently.

### C1 · <clarification name>
<The open question, the options if known, and who owns the answer. Owner: <team/person>.>

## Green Path

> The intended end-to-end journey, one scenario per step. Ref format `G-01`, `G-02`, …
> Every field is a single paragraph. `Rules:` lists the R-refs the scenario relies on.

### G-01 · <scenario title>
- **Subtitle:** <the one-line essence of this step>
- **Rules:** R1
- **Starting state:** <where the user/system is when this begins>
- **User actions:** <the exact click flow, arrows welcome>
- **Backend:** <what processes, in order — name payloads and systems>
- **Front-end result:** <what the user sees when it completes>
- **Next action:** <what the user does next, with the ref it leads to>
- **Expected outcome:** <what is now true, and which rule guarantees it>
- **Worked example:** <one concrete run with real-shaped numbers and names>

## Red & Edge Paths

> Failure and edge behaviours that branch off the Green Path. Ref format `E-01`, … Add
> `(open)` after the title for scenarios that hang on an unconfirmed C-item.

### E-01 · <edge scenario title>
- **Subtitle:** <what this protects against>
- **Rules:** R1
- **Starting state:** <the condition that triggers the edge>
- **User actions:** <what the user does — often the same as the happy path>
- **Backend:** <how the system detects and handles it>
- **Front-end result:** <what the user sees — never a silent failure>
- **Next action:** <how the user recovers or continues>
- **Expected outcome:** <what is protected, and which rule guarantees it>
- **Worked example:** <one concrete run>

<!-- graph-links:start — generated by tools/gen_graph_links.py; do not hand-edit -->
## Vault graph
[[discovery/definition/clauseiq-supplier-rounds|clauseiq-supplier-rounds]]
<!-- graph-links:end -->
